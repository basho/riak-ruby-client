describe "Yokozuna" do
  context "without any indexes" do
    it "should allow index creation"
  end

  context "with an index" do
    it "should have an index list"
    it "should allow index inspection"

    it "should associate an index with a bucket"

    context "associated with a bucket" do
        it "should index on object writes"
    end
  end
end
