describe "Yokozona queries with a schema and indexes" do
  it "should produce results on single term queries"
  it "should produce results on multiple term queries"
  it "should produce results on queries with boolean logic"
  it "should produce results on range queries"
  it "should produce results on phrase queries"
  it "should produce results on wildcard queries"
  it "should produce results on regexp queries"
  
  context "using parameters" do
    it "should support start/offset parameters"
    it "should support rows/limit parameters"
  end
end
