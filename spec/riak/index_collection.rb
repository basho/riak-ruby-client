require 'spec_helper'

describe Riak::IndexCollection do
  describe "json initialization" do
    it "should accept a list of keys"
    it "should accept a list of keys and a continuation"
    it "should accept a list of results hashes"
    it "should accept a list of results hashes and a continuation"
  end
end
